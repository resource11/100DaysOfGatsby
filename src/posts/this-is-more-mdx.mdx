---
title: "This is More MDX"
date: 2019-01-30
author: "Kathleen McMahon"
path: /this-is-more-mdx
featuredImage: ../images/disney-cruise-bar.jpg
---

import { graphql } from "gatsby"
import SEO from "../components/seo"
import ThisIsMdx from "./this-is-mdx.mdx"

export default ThisIsMdx

<SEO title={props.pageContext.frontmatter.title} />

<h1>Frontmatter title {props.pageContext.frontmatter.title}</h1>
<p>Frontmatter author{props.pageContext.frontmatter.author}</p>

---

## Can you write more MDX and import a separate MDX file?

Yes you can! This page is an MDX file that is importing another MDX file. This page uses a default export, so it won't take on the default layout set by the gatsby-config file.

This page is also exporting metadata through a const and through a GraphQL export query. Need to double check this one.

![Colorful group of umbrellas nestled on the soft tropical sands of Castaway Cay](https://res.cloudinary.com/kathleenmcmahon/image/upload/q_auto,f_auto/v1587501269/100-days-of-gatsby-cloudinary/castaway-key-umbrellas-closeup.jpg)

### And a CodePen embed

<CodePen codePenId="eYOQQxY" />

### Where is the imported shortcode?

It is imported right below this. Good times.

<!-- This default export overrides the default layout set in the
gasby-config.js file ensuring that the FAQ component isn't wrapped
by other elements -->

Passing children into the `<ThisIsMdx/>` component/shortcode below does nothing, because it is an MDX file.

<ThisIsMdx children={`Testing456`}>Testing 123</ThisIsMdx>

export default ({ children }) => <>{children}</>

export const query = graphql`
  query MyMoreMDXQuery {
    allMdx {
      edges {
        node {
          frontmatter {
            date(formatString: "MMMM DD, YYYY")
            title
            path
            featuredImage {
              id
            }
          }
          exports {
            metadata {
              author
              title
              path
              date
              featuredImage {
                id
              }
            }
          }
        }
      }
    }
  }
`

<!-- query MdxExports {
  allMdx {
    nodes {
      exports {
        metadata {
          name
          author
        }
      }
    }
  }
} -->
